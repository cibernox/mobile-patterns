import bezierEasing from 'mobile-patterns/utils/bezier-easing';

module('bezierEasing');

// Replace this with your real tests.
test('it works', function() {
  var result = bezierEasing();
  ok(result);
});
