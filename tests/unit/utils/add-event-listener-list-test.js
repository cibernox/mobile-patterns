import addEventListenerList from 'mobile-patterns/utils/add-event-listener-list';

module('addEventListenerList');

// Replace this with your real tests.
test('it works', function() {
  var result = addEventListenerList();
  ok(result);
});
